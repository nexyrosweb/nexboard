# Serveur API
PORT=3001
HOST=0.0.0.0

# Chemin relatif vers la base SQLite (depuis le dossier server/)
DATABASE_PATH=./data/nexboard.db

# URL de l'API côté frontend (vide = proxy Vite en dev via /api)
VITE_API_URL=
